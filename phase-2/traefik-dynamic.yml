tls:
  certificates:
    - certFile: /certs/gitlab.local.crt
      keyFile: /certs/gitlab.local.key
  stores:
    default:
      defaultCertificate:
        certFile: /certs/gitlab.local.crt
        keyFile: /certs/gitlab.local.key
